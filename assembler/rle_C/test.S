#include "printx.hs"

.global _Neg // объявление метки с именем подпрограммы.
.text
_Neg:
  ar5 = ar7 - 3; // адреса в стеке для доступа к входным параметрам.
  push ar0, gr0; // сохранение используемых в подпрограмме регистров.
  ar0 = [ar5]; // получение значения входного параметра.
  PRINTF1("=%x\n",ar0)
  gr1 = [ar0];
  PRINTF1("=%x\n",gr1)
  gr7 = 0;
  pop ar0, gr0; // восстановление значений регистров при выходе
  return;
